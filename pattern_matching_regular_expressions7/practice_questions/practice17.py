# What does the . character normall match? What does it match if re.DOTALL is passed as the second argument to re.compile?

# Matches any character except for a newline
# Matches any character, including newline