# What is the difference between range(10), range(0, 10), and range(0, 10, 1) in a for loop?

# The function outputs the same values, however, using two parameters defines the function range, while using three parameters defines the function range AND integer increase