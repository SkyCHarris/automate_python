# 5. If a citionary is stored in spam, what is the difference between the expressions 'cat' in spam and 'cat' in spam.keys()?

# spam = 'cat' is a list item, while spam.keys(cat) is the key value

# Actual
# There is no difference. The in operator checks whether a value exists as a key in the dictionary